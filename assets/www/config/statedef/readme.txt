Use the ANT script build.xml in order to compile the SCXML into JavaScript files.

NOTE: You need to set the appropriate values in build.settings first 
      (build.settings will be created as a copy of build.settingsDefault
       on first execution)